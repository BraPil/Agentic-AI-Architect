# Work Log

## Purpose

This file records substantial work completed in the repository so future contributors can understand what was done, why it was done, and what remains open.

## Work Ledger

| Date | Work Item | What Changed | Validation | Open Follow-Up |
|------|-----------|--------------|------------|----------------|
| 2026-06-30 | Rank-aware retrieval eval (the instrument) → hybrid reranking vindicated and enabled by default | Built `src/pipeline/ranking_metrics.py` (pure: MRR, nDCG@k with exponential gain, precision@k, hit@1, plus a **label-free graded relevance oracle** `grade_result` = +1 per matched ground-truth signal author/tool/topic → 0..3) and `scripts/eval_ranking.py` (runner + `--compare`; n_results=10). This is the instrument `run_eval.py` lacked. A/B over the 15 search questions: hybrid OFF→ON moved MRR 0.900→1.000, nDCG@10 0.8769→0.9469, P@5 0.840→0.907, hit@1 0.800→1.000; exact-term query eval-005 went rank-2→rank-1. On that evidence, flipped `AAA_HYBRID_RANKING` default OFF→ON in `LinkedInPersonaStore.search()` (kill-switch `=0` remains). Specs: `docs/ranking-aware-eval-v0.md`, `docs/hybrid-ranking-v0.md §6`. | 345 tests pass (+17 in `tests/test_ranking_metrics.py`: metrics + oracle, incl. the nDCG "promoting a relevant doc raises the score" property). Full suite green with hybrid now ON by default (no order-assuming test broke). A/B run saved (rank_off/rank_on) | Grow the question set with exact-term/keyword cases (eval-005 is the only clear one); re-tune α/selectivity on the larger set; consider a cross-encoder second stage measured on the same instrument; wire the rank metrics into CI alongside the pass-rate |
| 2026-06-30 | Retrieval ranking (b) slice 1 — hybrid lexical+vector reranking, built/tested/measured, shipped OFF by default | Built `src/pipeline/hybrid_ranking.py` (pure stdlib: `tokenize`, `bm25_scores`, `selective_terms`, `hybrid_scores`, `rerank_hits`): BM25 (IDF over the candidate pool) fused with vector sim as a convex blend `(1-α)·vector + α·lexical_norm` (α=0.25), used **only to re-order** — reported `score` stays vector similarity. Selectivity-gated so lexical contributes only from terms rare in the pool (df ≤ 50%); conceptual queries are an exact no-op. Wired into `LinkedInPersonaStore.search()` over the over-fetched pool; `rerank_by_outcomes` now bases ranking on `hybrid_score` when present so the recommendation path honors hybrid order. **Eval-gated decision:** baseline 15/15 @ avg-rel 0.4751 vs hybrid 15/15 @ 0.4143 — every judgment check unchanged on all 15 questions, so **zero quality change**; the 0.06 drop is a metric artifact (avg-rel = mean top-1 *vector* sim, which can only fall under any reorder). Conclusion: the eval can't measure ranking quality → shipped OFF (`AAA_HYBRID_RANKING=1` to enable). Spec: `docs/hybrid-ranking-v0.md`. | 328 tests pass (+17 in `tests/test_hybrid_ranking.py`: tokenize/BM25/selectivity/blend/rerank, incl. the deterministic lift proof — lower-vector exact match promoted to rank 1 — and the conceptual-query no-op). Eval run twice (ungated + gated) confirming no check regression | **Re-scoped (b): build a ranking-aware eval FIRST** — graded relevance + MRR/nDCG@k + exact-term/keyword questions — then justify a reranker (hybrid / cross-encoder / query expansion). The measurement instrument is the prerequisite |
| 2026-06-30 | Persona curation guard — bar non-practitioner personas at the ingest chokepoint | Built `src/pipeline/curation.py` (`PersonaCurationList` + `CurationEntry`, pure stdlib): a persisted JSON denylist (`data/curation_denylist.json`) of persona IDs barred from the store, with load/save/`is_blocked`/`block`. Wired enforcement into `LinkedInPersonaStore.ingest()` — a barred `persona_id` is skipped (logged WARNING, returns False) so **every** ingest path inherits the bar (same lowest-shared-layer pattern as the experimental-quarantine fix). Added `store.curation` property, `store.prune_persona()` (deletes a persona's posts, returns count), constructor injection for tests, and kill-switch `AAA_PERSONA_CURATION=0`. New CLI `scripts/prune_persona.py` does prune-and-bar as one atomic act (+`--list`/`--bar-only`/`--dry-run`). Seeded the denylist with the 9 known off-standard personas (5 April + 4 June). Spec: `docs/persona-curation-guard-v0.md`. **First live run caught the bug it was built for:** `kyle-faust` + `anthony-smith-mba-sta` were still in the committed 417 snapshot despite being recorded as pruned 2026-06-29 — pruned both, store 417→415, snapshot re-exported. | 311 tests pass (+13 in `tests/test_curation.py`: list roundtrip/sort/missing-file/malformed/empty-block-raises + store enforcement: blocked-skipped, allowed-ingested, kill-switch, prune-deletes, shipped-denylist-sanity). Live audit confirms 0 barred personas remain in the 415-item store; guard active (9 barred) | v1 candidates: ingest-time topical classifier as a second (suggest-only) signal; periodic automated store-vs-denylist audit (the 2026-06-30 manual check, automated) |
| 2026-06-29 | P6 outcome capture — slice 2 (signal now drives ranking; loop closed) | Built `src/learning/outcome_weighting.py` (pure functions: `rerank_by_outcomes`, `hit_multiplier`, `gated_multipliers`, `_normalize`) and wired it into `get_architecture_recommendation` via `_apply_outcome_weighting` in `src/api/mcp_server.py`: after `store.search()` and before synthesis, each hit is re-scored `relevance × outcome_multiplier` and re-sorted (stable). A hit's multiplier is the **mean** of its applicable entity multipliers (author persona + matched `mentioned_tools`), each bounded to [0.5,1.5] by slice 1 so the combination stays in range. **Minimum-evidence gate** (`MIN_EVIDENCE_DEFAULT=5`): an entity's multiplier only leaves neutral once it has ≥5 recorded outcomes, so re-ranking is an *exact no-op* on today's empty ledger — live and tested, but never reshapes retrieval on noise. Original `score` preserved; `outcome_multiplier`/`ranking_score` attached + an `outcome_weighting` audit block on the payload. Kill-switch `AAA_OUTCOME_WEIGHTING=0`. **Rail decision:** wired into Rail B (the ChromaDB recommendation path where outcomes are produced), NOT Rail A's `source-weighting-model-v2` (which weights `KnowledgeBase` source types, not personas) — see decision-log. | 298 tests pass (+15: 12 in `tests/test_outcome_weighting.py` covering normalize/gate/multiplier-mean/bounds/promote/demote/stable-noop + a seeded end-to-end lift proof; 3 in `tests/test_outcomes.py` for the MCP wiring + kill-switch). Lift proven deterministically: 5 adopted-and-worked outcomes for a persona move its lowest-relevance hit from last to first | Slice 3 candidates: segment-aware outcome multipliers (enterprise vs startup); recency decay on stale outcomes; surface per-entity track record in the payload. Live eval harness scores Rail A so it can't measure this rail — compare `active:true` vs `AAA_OUTCOME_WEIGHTING=0` ordering once real outcomes accrue |
| 2026-06-29 | P6 outcome capture — slice 1 (the missing half of the learning loop) | Built `src/learning/outcomes.py` (`RecommendationOutcomeStore` + `OutcomeRecord`): an append-only JSONL ledger (`data/recommendation_outcomes.jsonl`) of `recommendation` + `outcome` events, replayed into per-id merged views. `success ≡ adopted AND worked`. Aggregates overall adoption/success rates + per-persona/per-tool signal with Laplace-smoothed weight multipliers in [0.5,1.5]. Wired `get_architecture_recommendation` to stamp a `recommendation_id` + log the event; added MCP tools `record_recommendation_outcome` + `get_outcome_summary` and CLI `scripts/record_outcome.py`. Spec: `docs/p6-outcome-capture-v0.md`. **Scope: capture + expose only — signal is NOT yet applied to ranking (slice 2), so eval scores stay stable while the dataset is tiny; outcomes are human-reported (no self-grading).** | 283 tests pass (+14 in `tests/test_outcomes.py`: roundtrip, unknown-id rejection, score clamp, last-outcome-wins, adopted≠success, pending, aggregate/persona-signal, smoothing, malformed-ledger, MCP glue); CLI `--summary`/`--pending` verified | Slice 2: feed multipliers into `source-weighting-model-v2`, gate on ≥5 outcomes/entity, prove eval lift before/after |
| 2026-06-29 | Corpus re-curated + merged with CI weekly refresh — store reconciled to 417 (union) | Session-restart audit found the live store drifted to 385 vs the committed 381 snapshot; pruned 4 off-standard personal-contact/job-announcement personas (kyle-faust, paul-northrup-pe, matt-luke, anthony-smith-mba-sta) — the class deliberately pruned in April ("store holds only AI/tech practitioners") that re-entered via the June reactions scrape. On push, discovered the automated GitHub Actions weekly refresh (8b4ffb2) had committed a **regressed** 334-item snapshot — its run rebuilt from a partial base and dropped 83 local items (79 LinkedIn persona image/text/video/article + 2 P6 learning_artifact + 2 blog), while adding 36 fresh (27 arXiv, 7 project_learning, 2 blog). Resolved as a **union, not a clobber**: added CI's 36 fresh items into the local 381 store via the incremental restore script → 417; kept CI's fresh eval_results.json. | store 417 == snapshot 417; 4 pruned absent; all 117 blog_post + 2 learning_artifact retained; CI's 27 arXiv merged; 265 tests pass | (1) CI weekly refresh regresses the corpus — it must restore from the committed snapshot before ingesting, else it keeps dropping the persona corpus; (2) CI commits only snapshot+eval, not the binary store, leaving them inconsistent; (3) ~some CI arXiv items look off-topic — review arXiv query relevance |
| 2026-04 | Sprint 5 kickoff — schema versioning, REST v1, webhook system | Added `schema_version: "1.0"` to all 3 MCP tool responses; built `src/api/rest_v1.py` (7 HTTP routes wrapping ChromaDB: /health, /search, /recommend, /trending-tools, /personas, /webhooks/*); built `src/api/webhook.py` (subscriber registry + tool-change alert delivery); wired webhook notification into `refresh_corpus.py` tool-rank diffing; mounted v1 router in `rest.py` | v1 router smoke-tested: all 7 routes register cleanly; webhook registry loads; schema_version confirmed on all MCP payloads | Register ExMorbus V3 as a webhook subscriber once its endpoint is live; validate < 200ms cached query latency |
| 2026-04 | Sprint 4 closed — GitHub Actions weekly refresh + all gaps addressed | Added `.github/workflows/refresh_corpus.yml` (weekly Monday 06:00 UTC: blog + arXiv ingest → reenrich → eval → commit snapshot); Sprint 4 complete: blog expansion (Chip Huyen, Sebastian Ruder, Eugene Yan), freshness filter, synthesis eval (20/20 100%), persona coverage report, auto-refresh script, re-enrichment script | Workflow file committed; eval 20/20 with synthesis at high/medium confidence; corpus 260 items fully enriched | Monitor first automated workflow run after secret ANTHROPIC_API_KEY is added in GitHub repo settings |
| 2026-04 | Blog corpus fully enriched — 105 posts re-enriched in-place | Rebuilt `scripts/reenrich_blogs.py` to use ChromaDB `collection.update()` in-place rather than delete+refetch; fixed persona_id hyphen/underscore mismatch; enriched all 105 blog posts (Simon Willison, Lilian Weng, Chip Huyen, Sebastian Ruder, Eugene Yan) with Claude Haiku topics/tools/claims | 105/105 re-enriched, 0 failed; eval recovered to 20/20 after enrichment; synthesis fixed (markdown fence stripping in `_synthesize()`) | Rerun after any batch text-only ingest |
| 2026-04 | Sprint 4 Sprint — blog expansion + synthesis eval + auto-refresh | Added Chip Huyen, Sebastian Ruder, Eugene Yan to BLOG_REGISTRY (+45 posts, 260 total); MCP search_knowledge gains min_date filter and JSONL usage log; 5 synthesis eval questions (eval-s01..s05); persona coverage report (--coverage); refresh_corpus.py with daemon mode; reenrich_blogs.py | 260 items indexed; eval 20/20 (100%) after fixes | Run GitHub Actions workflow weekly |
| 2026-04 | LinkedIn Chrome extension iteratively hardened to v22 | Built MV3 Chrome extension (`extensions/linkedin-exporter/`) across 22 versions; added rolling scrape with age cutoff, DOM-occlusion-aware final-top pass, author extraction from leaf spans, and `requireVersion` gate; v22 recovers posts occluded during downward virtual-DOM scrolling | Live extension captured 92 posts across multiple scrape sessions; extension version gate prevents stale-content runs | Evaluate whether periodic background refreshes can replace manual click-to-scrape |
| 2026-04 | LinkedIn export pipeline built — 92 posts ingested into ChromaDB persona store | Built `scripts/process_linkedin_export.py`; ingests Chrome extension JSON exports → raw `.md` artifacts → `research_sources.json` → ChromaDB `linkedin_reactions` collection; Claude Haiku extracts directClaims, inferredBeliefs, mentionedTools, topics, voiceSignals, summary per post | 92 posts indexed; all enriched with Claude extraction; `LinkedInPersonaStore` confirmed 120 total items post-enrichment | Add batched re-enrichment path for posts that were added text-only before API key was available |
| 2026-04 | YouTube transcript pipeline completed and wired to ChromaDB | Built `src/pipeline/youtube_ingest.py` (two-stage: youtube-transcript-api → yt-dlp fallback); wired auto-call to `extract_transcript_sources.py` after fetch; fixed yt-dlp subtitle command (removed invalid comma-separated lang codes); ingested 20 transcripts (10 Karpathy, 10 Cole Medin) into ChromaDB with `post_type=youtube_transcript` | 20 YouTube entries in ChromaDB, all Claude-enriched; `data/wiki/raw/karpathy/` and `data/wiki/raw/cole_medin/` populated | Automate channel discovery to keep video list fresh as creators publish |
| 2026-04 | Mitko Vasilev GitHub repos ingested into persona store | Built `src/pipeline/github_ingest.py`; selected 5 most significant repos (vllm-turboquant 540★, ai-coding-factory 152★, rlmgw 122★, firecracker-agentfs, ccar) via GitHub API; wrote READMEs to `data/wiki/raw/mitko_vasilev/`; extracted structured research sources | 6 GitHub README entries in ChromaDB for Mitko Vasilev persona | Expand to Paolo Perrone and Stanislav Beliaev repos if public GitHub presence grows |
| 2026-04 | Personal-contact personas pruned from ChromaDB | Removed 5 personal-contact personas (ANS, Alex Dobrenko, Jeneé O., Karen Robes Meeks, Alex Arend) from the `linkedin_reactions` ChromaDB collection; store now contains only AI/tech practitioners | Confirmed 50 personas post-pruning; store count consistent | None |
| 2026-04 | Full-corpus Claude enrichment pass completed | All 120 ChromaDB items enriched with Claude Haiku extraction (directClaims, mentionedTools, topics, voiceSignals, summary); items initially added text-only were deleted and re-ingested with structured metadata; `build_document()` now includes extracted fields in the embeddable string | 120/120 items have non-empty `mentioned_tools` and `summary` metadata fields | Monitor for extraction quality drift as new content types are added |
| 2026-04 | ChromaDB persistence solved — export/restore scripts + committed snapshot | Built `scripts/export_chromadb_snapshot.py` and `scripts/restore_chromadb_snapshot.py`; 204-item snapshot committed to `data/wiki/schema/chromadb_snapshot.json` (937 KB); snapshot auto-updates after every ingest run; added cookie file patterns to `.gitignore` | Restore dry-run confirmed all 204 IDs would be re-indexed; snapshot round-trips verified | Re-export snapshot after every significant ingest batch |
| 2026-04 | Blog ingest pipeline built — Simon Willison + Lilian Weng (60 posts) | Built `src/pipeline/blog_ingest.py` with RSS/Atom feed parser, stable MD5-based post IDs, Claude Haiku extraction, and raw artifact writes; added `scripts/ingest_blogs.py` CLI; ingested 30 Simon Willison + 30 Lilian Weng posts, all Claude-enriched | 60 posts indexed; blog_post post_type confirmed in ChromaDB; stable IDs verified across process restarts | Add more blog sources (Chip Huyen, Sebastian Ruder, etc.) as Sprint 2 expands |
| 2026-04 | arXiv pipeline built — 24 papers across 7 curated queries | Built `src/pipeline/arxiv_ingest.py` with arXiv Atom API; 7 queries covering agentic AI, RAG, memory, evaluation, code gen, CoT, and fine-tuning; Claude extraction per abstract; added `scripts/ingest_arxiv.py` CLI | 24 unique papers indexed as arxiv_abstract post_type; dedup across queries working | Expand queries or increase max_results as corpus grows |
| 2026-04 | Evaluation backbone built — 15 ground-truth questions, 100% pass rate | Built `data/wiki/schema/eval_ground_truth.json` (15 questions with expected persona, tools, topics, relevance threshold); built `scripts/run_eval.py` scoring harness; first run: 15/15 pass (100%), avg top relevance 0.465 | All 15 questions pass with current 204-item corpus | Re-run eval after every major corpus change to catch regressions |
| 2026-04 | Sprint 1 MCP server built — search_knowledge, get_architecture_recommendation, get_trending_tools | Built `src/api/mcp_server.py` using FastMCP (mcp>=1.0.0); exposes 3 tools: semantic search with persona filter, Claude Haiku synthesis of architecture recommendations, and tool mention frequency ranking; graceful fallback when no API key; added `scripts/run_mcp_server.sh` launcher and `claude_desktop_config.json` registration snippet | Server boots, connects to 120-item ChromaDB, all 3 tools verified; tool registration confirmed via `mcp._tool_manager.list_tools()`; search returns semantically relevant results | Connect to Claude Desktop locally; Sprint 2: expand knowledge base with blogs/arXiv/GitHub trending |
| 2026-04 | ExMorbus operator console and consolidated status API added | Added a dynamic `/operator` workbench page, a no-store `/api/operator` endpoint, and a shared frontend loader that composes runtime state, evaluation history, analytics summary, controller decisions, dead-letter pressure, and cycle-ledger summary into one operator read-model | `cd workspaces/ExMorbus-v0.1/apps/web/frontend && npm run type-check && npm run build` passed | Decide whether to add alert delivery and periodic refresh/polling on top of the operator read-model |
| 2026-03 | Foundation merged to `main` | Merged the starter system foundation including docs, agents, knowledge layer, pipeline, tests, and instruction files | `pytest tests/ -v` passed with 119 tests | Reconcile roadmap and implementation priorities with the more focused MVP direction |
| 2026-03 | Architecture review docs merged to `main` | Added independent review docs and multi-model review log | Merge completed cleanly; test suite remained green after merge | Resolve remaining architectural questions in a more compact operational format |
| 2026-03 | Repository boundary and ecosystem sequencing memos added | Added product-repo boundary decision and ecosystem sequencing guidance | Docs-only change | Convert sequencing guidance into a shared substrate spec and revised MVP roadmap |
| 2026-03 | Repository memory system added | Added instruction hierarchy, memory protocol, work index, and indexed ledgers for decisions, discoveries, lessons, and work | Docs-only change | Keep logs updated during future meaningful work |
| 2026-03 | Influencer tracker added | Added a durable influencer and influential-post registry seeded with user-provided LinkedIn profiles, company feeds, and post URLs | Docs-only change | Enrich the tracker with reviewed summaries, tags, and source-quality assessments once content is accessible |
| 2026-03 | Influencer tracker enriched and structured | Added reviewed notes for a public source, split sources into watchlist tiers, and created a machine-ingestable YAML registry for future research pipeline use | Public article metadata was fetched and incorporated; docs-only change | Continue enriching auth-blocked LinkedIn entries as public mirrors or authenticated access become available |
| 2026-03 | Influencer review cards expanded across all captured resources | Added review-card entries for profiles and posts across the full current source set, using evidence-bounded summaries for auth-blocked LinkedIn resources | Docs-only change | Replace conservative working reviews with direct content summaries as authenticated or public access becomes available |
| 2026-03 | Answer contract and cycle spec added | Added the first canonical answer-contract document and a refined research-and-training cycle specification aligned to the original prompt and the current enterprise-first corpus direction | Docs-only change | Convert the contract into typed schemas and build the first scored evaluation set |
| 2026-03 | Typed answer contract models added | Implemented Pydantic request and response schemas for the first answer contract and added validation tests for defaults, serialization, and failure cases | `pytest tests/test_contracts.py -v` passed with 9 tests | Integrate the contract into the future API surface and evaluation pipeline |
| 2026-03 | Minimal REST API surface added | Added a FastAPI app with health, trends, tools, and contract-backed query endpoints, plus API tests and runtime dependencies | `pytest tests/test_api.py tests/test_contracts.py -v` passed with 14 tests | Extend the query path with stronger provenance, namespace-aware ranking, and eventual auth |
| 2026-03 | Framework and phase-report API endpoints added | Extended the REST API with framework matrix and phase-report endpoints backed directly by repository docs, and expanded API tests and README examples | `pytest tests/test_api.py tests/test_contracts.py -v` passed with 17 tests | Decide when static report serving should evolve into generated report artifacts or cached summaries |
| 2026-03 | Query ranking and framework filters improved | Added lightweight keyword-plus-confidence ranking for query results, optional evidence provenance fields, and server-side framework filtering by search and trajectory | `pytest tests/test_api.py tests/test_contracts.py -v` passed with 20 tests | Decide whether to add true full-text search or semantic retrieval before broadening the query interface |
| 2026-03 | Initial evaluation set implemented | Added a typed v0 evaluation-set contract, exposed it through the API, and documented the first scored question backbone for answer quality checks | `pytest tests/test_api.py tests/test_contracts.py tests/test_evaluation_set.py -v` | Decide whether future eval runs should stay static fixtures or evolve into executable scoring harnesses |
| 2026-03 | Executable query scoring harness added | Added a deterministic scoring harness for actual `/query`-style responses, exposed it at `/evaluate/query`, and fixed the root route so browser access no longer returns a misleading 404 at `/` | `pytest tests/ -q` passed with 146 tests before this follow-up; revalidate after harness changes | Decide when to augment deterministic scoring with LLM-judge or replay-based evaluation |
| 2026-03 | Retrieval fallbacks and aggregate eval added | Added token-aware KB retrieval, repo-native fallback sources with explicit provenance, and an aggregate `/evaluate/query-set` endpoint that scores filtered eval-question batches end-to-end | `pytest tests/ -q` | Decide whether to persist evaluation runs and trend them over time |
| 2026-03 | Evaluation persistence and source weighting added | Added persisted evaluation-run storage in SQLite, a history endpoint for evaluation runs, and an explicit v0 source-weighting model used during retrieval ranking | `pytest tests/ -q` | Decide whether to derive future source weights from historical evaluation outcomes |
| 2026-03 | Enterprise overlay fields and eval performance summary added | Added machine-readable enterprise overlay fields to the canonical answer contract, scored them in the evaluation harness, and exposed a compact `/evaluate/performance` summary over persisted evaluation runs | `pytest tests/ -q` | Decide whether segment-aware evaluation should get its own dedicated question subset in v2 |
| 2026-03 | Learned weighting, freshness ranking, and segment-aware eval added | Added v2 learned source multipliers from persisted evaluation history, freshness-aware ranking tied to time horizon, and a `/evaluate/query-segments` endpoint for segment comparison over the same query | `pytest tests/ -q` | Decide whether to graduate the shared question set into a segment-native evaluation corpus |
| 2026-03 | LinkedIn PDF ingest pipeline operationalized on full export | Added deterministic PDF text and OCR ingest for LinkedIn exports, improved author/body parsing heuristics, and generated a centralized artifact bundle from a 158-page export with 60 detected posts | `pytest tests/ -q` passed with 172 tests; ingest output written to `data/ingests/Agentic_AI_Architect_Ingest_20260314/` | Improve repost/quoted-post separation and add rendered-page OCR fallback when embedded images are missing |
| 2026-03 | High-signal LinkedIn ingest filtering and KB seeding added | Added relevance scoring for AI/ML/agentics insight, filtered low-signal social posts, and seeded the top 25 retained posts from the 158-page export into the SQLite knowledge base as durable `linkedin_pdf_post` entries | `pytest tests/ -q` passed with 176 tests after change; seeded run retained 54 of 60 posts and wrote 25 KB entries | Tighten insight scoring over time and surface filtered-out reasons in a review artifact |
| 2026-03 | ExMorbus-through-AAA operating model and MoltBook-style PoC scope added | Added a durable operating model for steering ExMorbus through AAA and a scoped PoC spec covering the training funnel, modified social mechanics, agent-routing loop, and acceptance criteria | Docs-only change | Draft the shared substrate candidate spec and the first shell ontology doc that the ExMorbus PoC should validate |
| 2026-03 | Shared substrate candidate spec added | Added the first explicit candidate spec for reusable cross-system contracts spanning provenance, confidence, evaluation, event envelopes, namespace progression, and task/result shapes | Docs-only change | Use the ExMorbus PoC to validate which contracts deserve extraction and which remain system-specific |
| 2026-03 | ExMorbus shell ontology added | Added the first implementation-agnostic shell ontology covering agents, capabilities, opportunities, tasks, artifacts, evaluations, feedback loops, score surfaces, and training-funnel objects | Docs-only change | Convert the ontology into typed schemas or DTOs once the ExMorbus PoC begins validating which objects survive contact with implementation |
| 2026-03 | Earlier docs-session findings integrated into ExMorbus-through-AAA specs | Updated the ExMorbus operating model, PoC scope, shared substrate candidate, and shell ontology so they explicitly reuse AAA's prior work on canonical contracts, structured evaluation, learned weighting, and durable repo memory | Docs-only change | Translate the now-aligned docs into typed ExMorbus shell contracts once the PoC implementation starts |
| 2026-03 | Branch strategy summary added | Added a durable summary of how `main`, `feature`, `task`, `research`, and `spike` branches should relate now that the foundation and first research-review layers are both merged | Docs-only change | Use the summary to keep future ExMorbus and shared-substrate work from drifting into unbounded branch sprawl |
| 2026-03 | Roadmap reconciled to current execution direction | Updated the implementation plan and canonical repo instructions so they explicitly reflect the earlier pull-forward of contract, evaluation, query-surface, weighting, and curated ingest work, while keeping orchestration strategy marked as an open decision | Docs-only change | Decide whether orchestration will remain custom or be formalized around LangGraph before broadening runtime complexity |
| 2026-03 | AAA versus ExMorbus ownership matrix added | Added a durable ownership matrix with examples and inference rules so reusable architectural contracts stay in AAA, medical-domain specifics stay in ExMorbus, and shared candidates are still designed in AAA first | Docs-only change | Use the matrix to drive the first typed shell split between reusable shell contracts and ExMorbus-only enrichments |
| 2026-03 | AAA versus ExMorbus ownership refined around runtime and knowledge ownership | Clarified that AAA designs and learns from ExMorbus architecture, while ExMorbus owns its MoltBook-like environment, domain discoveries, and dissemination interfaces | Docs-only change | Use this refinement when drafting the first typed reusable shell contracts so AAA stores architectural performance feedback rather than ExMorbus domain knowledge |
| 2026-03 | Mouseion named and first shell contracts started inside AAA | Named the shared substrate `Mouseion`, added Mouseion Core and ExMorbus-only boundary docs, and implemented the first typed reusable shell contracts inside `src/contracts/mouseion.py` | Contract tests and editor validation | Extend the first Mouseion contracts only after ExMorbus begins implementing against them in its own repo |
| 2026-03 | ExMorbus runtime boundary mapped to Mouseion and first adapter layer added | Mapped current ExMorbus runtime classes to Mouseion shell objects from code, then added an ExMorbus-side adapter that emits Mouseion-compatible payloads and validates them against AAA contracts when available | Focused ExMorbus adapter tests and cross-repo docs | Start using the adapter at real ExMorbus runtime boundaries instead of only in isolated conversion tests |
| 2026-03 | Hardware scaling plan added | Added a comprehensive hardware plan covering the 3-month 24/7 discovery sprint at three scales (Tier 1: 100–200 agents / ~$27K, Tier 2: 1,000–2,000 agents / ~$105K, Tier 3: 10,000–20,000 agents / ~$430–$595K) with specific hardware recommendations (DGX Spark, Mac Mini M4 Pro, RTX 5090), power budgets, operational cost estimates, upgrade triggers, and a recommended month-by-month starting plan | Docs-only change | Validate actual GPU utilisation and SQLite migration trigger point during Month 1 of the sprint |
| 2026-03 | AAA-side ExMorbus state gap analysis added | Added a durable AAA assessment of where ExMorbus is now, where it wants to go, the gap between validated shell and validated workflow, and the next artifact set AAA should ask ExMorbus to produce | Docs-only change | Revisit after ExMorbus implements its first query-to-result workflow and compare actual shell pressure-test findings against this analysis |
| 2026-03 | Local ExMorbus cycle-history workbench slice added and automated | Added generated cycle-history artifacts, `/cycles` and `/cycles/[cycleId]` workbench routes, cycle-history API endpoints, home-feed cycle-ledger surfacing, and a Python pipeline path that emits `cycle-history.json` alongside the snapshot instead of relying on hand-maintained ledger data | `npm run build`, `npm run type-check`, regenerated snapshot and cycle-history artifacts, and browser validation of `/`, `/cycles`, and `/cycles/cycle-workbench-live-cycle` with no console errors | Replace file-backed cycle history with a native run ledger once PostgreSQL-backed persistence is implemented |
| 2026-04 | ExMorbus autonomy tranche and system compendium documented | Consolidated the ExMorbus bounded autonomy stack into a single compendium covering native truth path, scheduler guards, recovery controls, bounded runtime, cross-cycle evaluation, evidence-based routing, analytics hardening, usage guidance, runtime artifacts, Mermaid diagrams, and knowledge graphs | `PYTHONPATH=/workspaces/Agentic-AI-Architect/workspaces/ExMorbus-v0.1 pytest workspaces/ExMorbus-v0.1/tests/test_autonomous_cycle.py workspaces/ExMorbus-v0.1/tests/test_workbench_snapshot.py -v` passed with 29 tests; live bounded runtime emitted aligned runtime-state, evaluation-history, and analytics-summary artifacts | Promote the new compendium into a frontend or API-facing operator reference and decide whether to add alert delivery on `watch` and `needs-attention` |
| 2026-04 | ExMorbus operator runbook added | Added a task-oriented operator runbook covering bounded runtime modes, first-check artifacts, health interpretation, triage tables, routing investigation, and recovery playbooks aligned to the autonomy stack | Docs-only change built on the already-validated bounded autonomy stack and compendium | Fold the runbook guidance into the live operator console without duplicating maintenance burden |
| 2026-04 | ExMorbus operator console surface added | Added a live `/operator` workbench page, a no-store `/api/operator` endpoint, and a `loadAutonomyOperatorPayload` frontend loader composing runtime state, evaluation history, analytics summary, controller decisions, dead-letter pressure, and cycle-ledger summary into one consolidated read-model | `npm run type-check && npm run build` passed; build output included `/operator` as a dynamic route | Add auto-refresh and surface a compact status card on the home page |
| 2026-04 | ExMorbus operator console auto-refresh and home-page status card added | Added a 30-second `AutoRefresh` component mounted on the operator console page, and an `AutonomyStatusCard` component mounted above the home-page feed showing health / runtime status / weakest mission with a direct link to the operator console; stale-runtime state already surfaced via "Runtime fresh"/"Runtime stale" MetaPill on the operator console | `npm run type-check && npm run build` passed; 29 Python regression tests passed | Decide whether to add proactive alert delivery for watch and needs-attention health states |
| 2026-04 | Orchestration strategy spike completed | Compared Archon, LangGraph, and extended BaseAgent for Phase 4; recommended BaseAgent extension through Phase 3, LangGraph migration at Phase 4 start, Archon reserved for Phase 6 self-improvement loop; documented LLM Wiki architecture influence on KB index-before-FAISS sequencing | Docs-only change; all decisions recorded in spike doc | Implement asyncio parallel step for TrendTracker + ToolDiscovery as first concrete pull-forward |
| 2026-04 | ExMorbus operator alert banner added | Added an `AutonomyAlertBanner` component that renders a colour-coded (amber/watch, rose/needs-attention) full-reasons banner at the top of the operator console when health is not healthy; banner is absent during healthy operation so there is no noise in normal state | `npm run type-check && npm run build` passed | Decide whether to extend alert delivery to external channels (webhook, email) once the in-page surface proves sufficient |
| 2026-04 | Orchestrator parallel execution and retry added | Added `_run_parallel_analysis()` running TrendTracker + ToolDiscovery concurrently via `ThreadPoolExecutor`; added `_run_agent_step()` with exponential-backoff retry checking `AgentStatus.ERROR`; 3 new tests | `pytest tests/ -q` passed with 184 tests | APScheduler integration for Phase 4.2 recurring cycles |
| 2026-04 | LLM Wiki pattern implemented for AAA; GitHub + YouTube ingest pipelines built | Applied Karpathy's three-layer wiki (raw/ → pages/ → schema/) to AAA's knowledge layer; built `src/pipeline/github_ingest.py` and `src/pipeline/youtube_ingest.py`; ingested 8 priority GitHub READMEs (karpathy: nanoGPT/micrograd/makemore/autoresearch; coleam00: dark-factory/Archon/adversarial-dev/second-brain-starter); wrote 12 synthesized wiki pages and 2 typed schema files; YouTube transcripts deferred — cloud IP blocked, IDs queued in persona pages | `pytest tests/ -q` passed with 202 tests; all 8 raw artifacts confirmed in data/wiki/raw/ | Run YouTube transcript ingest locally to complete persona KB; build wiki index query endpoint (P2.3) |
| 2026-06-28 | World-class CLAUDE.md template ecosystem built | Created 16-file AI Engineering Operating System template at `docs/templates/claude-md/`: root CLAUDE.md (~150 lines) with Architectural Constitution (8 priority-ranked values), 5-block initialization survey that self-destructs after use, 6 companion docs (architecture, governance, workflows, evaluation, security, knowledge), 5 document templates (ADR, runbook, design-doc, incident, RFC), 3 mandatory memory files (decision-log, discovery-log, lessons-learned). Design produced through adversarial Board of Governors session across 6 indexed personas. Wiki page `pages/pattern_claude_md_ecosystem.md` filed at `reviewed` status. | Files committed and pushed (bf12591) | Wire template into onboarding flow for new projects; promote initialization survey to Claude Code slash command |
| 2026-06-28 | Internal project memory made searchable — project learning logs indexed into ChromaDB | Diagnosed that AAA was recording (decision/discovery/lesson logs) but those logs were invisible to search. Built `src/pipeline/project_learning_ingest.py` (markdown table parser → `LearningEntry` dataclass → ChromaDB upsert with `post_type=project_learning`); added `scripts/ingest_project_learnings.py` CLI; wired as step 1 of `refresh_corpus.py`. NOTE: this is **retrieval over internal memory**, a prerequisite for the learning loop — NOT outcome-feedback learning. True learning (recommendation → outcome capture → adjusted behavior) remains P6 and is unbuilt. Original commit (965b36d) overstated this as "closes the feedback loop"; corrected here. | 57 entries indexed (23 decisions, 23 discoveries, 11 lessons); corpus 120 → 179; 243 tests pass | Build P6 outcome capture: record whether a recommendation was adopted and whether it worked, then weight retrieval/eval by that signal |
| 2026-06-29 | LinkedIn exporter v1.1 — absolute timestamps + batch mode | content.js v23: decode absolute timestamps from the activity URN (snowflake: id>>22 = ms epoch; drift-proof, no DOM selector) → posts now carry `published_at` (ISO) + `published_ms`; validated against real export IDs (decode → correct Jun 23–28 dates). Age cutoff now uses the decoded date, fixing the empty-timestamp gap so "last N months" filtering works. background.js: BATCH MODE — walks a list of profile handles through each `/recent-activity/all/` (navigate→settle→inject→scroll+scrape→download→next) in the user's authenticated tab, with polite delays; manifest gains `tabs` permission. popup gains a batch textarea + progress. process_linkedin_export.py now prefers `published_at` for the store timestamp. | All JS `node --check` clean; manifest valid; URN decode validated on real IDs | Automation is in-browser only by design — headless/server-side LinkedIn scraping is auth-walled/IP-blocked/ban-risky; documented in README |
| 2026-06-29 | LinkedIn June reactions export ingested (+~75 posts) + recurring dedup bug fixed at source | User ran an authenticated Chrome-extension scrape (106 posts, 206KB) of their recent-activity reactions feed. First ingest added only 4 — traced to the `research_sources.json` dedup bug (script skipped posts present in that historical superset even when absent from the rebuilt store; this is the 2026-04 lesson recurring in `process_linkedin_export.py`). Fixed: dedup the SKIP decision against the STORE (authoritative target), keep research_sources.json only for append-dedup. Re-ran → store 302→381. Fresh content for several of the 15 targets (Paolo 11, GenAI Works 5, Stanislav 8, Nikki 3, Allie 3, Mitko 17, + Bajaj/Arpit/Ouri/Brenn/Ohaike). | store 381; snapshot exported; 82 new raw post files | 5 of 15 absent from this export (Lenny, Karpathy, Priyanka + the two whose token I conflated); timestamps came through empty so no hard 2-month filter |
| 2026-06-28 | Corpus strengthened — full blog corpus re-ingested (187 → 302 items) | Diagnosed the recurring weak-grounding root cause: the live store had ZERO blog_post entries (the deep technical corpus had been dropped from the snapshot). Re-ingested all 5 blog sources via the existing pipeline with Claude enrichment: Simon Willison 30, Lilian Weng 30, Chip Huyen 10, Sebastian Ruder 15, Eugene Yan 30 = 115 posts. Store 187 → 302 (235 external / 65 internal / 2 grounded). Measured grounding gains on previously-weak topics: prompt-injection top-3 went from noise to real Willison content ("2,000 people tried to hack my AI assistant", "Prompt Injection as Role Confusion"); RL/reward-hacking went from near-zero coverage to 0.65 grounding (Weng's "Reward Hacking in RL" now the top hit). | Snapshot 302 items committed; grounding improvements verified by before/after retrieval | Retrieval RANKING/density is the next lever (see discovery-log); consider reranking or hybrid keyword+vector for dense topical grounding |
| 2026-06-28 | OAA PR #1 merged; AAA pinned to merged-main + default cognition model → Sonnet | OAA merged PR #1 (cognition bridge + packaging fix) to main (604b939), added CI (py3.11/3.12, green) and fixed a real httpx test-dep bug; 251 OAA tests pass, `oaa-learning-cycle` console script live. AAA side: re-pinned requirements.txt to the merged-main commit (reproducible install; no release tags yet); bumped `run_learning_cycle.py` default model haiku → claude-sonnet-4-6 (learning artifacts are promoted into the permanent corpus, so quality > per-run cost). Re-validated against merged-main: cycle ran via the installed package, firewall held on a weakly-grounded question (synthesis 0.30, correctly blocked, no fabrication). | 265 AAA tests pass; install + no-collision re-verified against merged main; --no-harvest validation (corpus unchanged at 187) | OAA offered structured-output cognition for the bridge (more robust JSON) on a branch — take up if parsing proves fragile; bump pin when OAA tags a release |
| 2026-06-28 | OAA packaging fixed upstream + AAA rewired to the pip-installed package | OAA team (Opus) ran the integration prompt; PR #1 fixes packaging (src-layout `organic_agentic_autodev` + `oaa-learning-cycle` entry point) and enriches cognition (multi-provider, cognitive_cell, autoresearch, dashboard). Verified from AAA: `pip install git+…@<PR#1 head>` works, `import organic_agentic_autodev` does NOT collide with AAA's `src/`, run-cycle CLI contract unchanged. Rewired `scripts/run_learning_cycle.py` to invoke the installed package by default (`--oaa-path` now a dev override); updated requirements.txt pin. Loop re-proven against the installed package: a fresh cycle's synthesis earned 0.703, promoted, raised answer relevance 0.4617 → 0.7229 (+0.261). | 265 tests pass; pip install + no-collision verified; second slice-3 proof; corpus 186 → 187 (2 grounded artifacts) | Merge OAA PR #1; switch requirements pin SHA → @main/tag |
| 2026-06-28 | Learning loop slices 2–3 — cognition bridge + live cycle PROVES the loop changes AAA's answers | Built the cognition bridge in OAA (`src/cognition/`: Researcher→Critic→Synthesizer cycle with real LLM, KnowledgeRecordV0 emitter, CLI) — committed locally, preserved as `docs/oaa-patches/0001` (OAA push 403'd: token scoped to AAA). Built AAA orchestrator `scripts/run_learning_cycle.py` (seed+ground → OAA subprocess → harvest), honoring the AAA→OAA→AAA boundary. Confidence is adversarially earned (mean critic score), not self-rated. Ran live cycles: 3 researchers/2 critics/1 synthesizer scored 4 artifacts 0.64–0.72; exactly one cleared the 0.70 gate; promoted via CLI. **Slice 3 proof**: best answer relevance for the question rose 0.6687 → 0.7771 (+0.108) — AAA measurably got smarter by running its personas through OAA's regulated MoltBook. Added eugene-yan + sebastian-ruder as researcher personas in the seeder. | 265 tests pass; live before/after measured; final corpus 186 (1 promoted grounded artifact retained, 3 un-promoted rejected) | Slice 2.5 = OAA packaging fix (apply patch, resolve `src/` collision) so AAA can pip-depend on OAA; richer multi-cycle runs; grounding currently limited by 185-item local store |
| 2026-06-28 | Store-layer quarantine bug found & fixed during slice-3 verification | The `experimental` quarantine was enforced only in the MCP `search_knowledge` wrapper, NOT in `LinkedInPersonaStore.search()` — so direct callers (grounding, eval, persona tools) saw unpromoted artifacts. Moved quarantine into the store layer (post-filter, version-safe vs ChromaDB `$ne` dropping field-less docs) with `include_experimental` passthrough. This is the same "enforce at the layer, not per-caller" lesson from the source_tier work. | 265 tests pass; verified experimental hidden by default, visible only on explicit opt-in | — |
| 2026-06-28 | Learning loop slices 0–1 — regulated OAA artifact pipeline | Built `src/learning/` (promotion.py `PromotionGate`, harvester.py, seeder.py) implementing the regulated bridge to Organic Agentic AutoDev. Added third/fourth provenance tiers `experimental` (quarantined agent output) and `grounded` (human-promoted); search_knowledge quarantines experimental behind `include_experimental` flag; get_trending_tools + synthesis exclude/relabel all non-external tiers. Added 4 MCP tools (list/promote/reject/demote, `src/api/learning_tools.py`) + CLI (`scripts/promote_learnings.py`). Harvester consumes OAA's real KnowledgeRecordV0 shape (fixture-tested) and sanitizes content. Seeder maps AAA personas → OAA genomes (AAA→OAA influence). | 265 tests pass (22 new in test_learning.py); live ChromaDB integration verified (harvest→quarantine→promote→tier change); MCP server registers all 4 tools | Slice 2 = cognition bridge (real LLM research in OAA); resolve OAA `src/` packaging collision before live import |
| 2026-06-28 | Adversarial (Opus) review + hardening of the project learning ingest | Reviewed the Sonnet-built ingest and found 5 issues: (1) "feedback loop closed" overstated; (2) internal notes polluted `get_trending_tools` with self-references (ExMorbus ×11, ChromaDB ×7); (3) synthesis mislabeled AAA as an external "thought leader" in personas_cited; (4) zero tests despite CLAUDE.md P4; (5) month-only timestamps silently broke the `min_date` lexical filter. Fixes: added `source_tier: internal` metadata; excluded internal tier from trend aggregation (mcp_server + refresh_corpus); tiered synthesis evidence with new `internal_priors_applied` field so AAA priors are institutional memory, never cited as external authority; added `_normalize_date` (timestamps now full YYYY-MM-DD); wrote `tests/test_project_learning.py` (23 tests). Re-ingested to apply new metadata via stable-ID upsert (10 added, 47 updated, 0 orphans). | 243 tests pass; trend analysis back to 120 external items (0 internal leak); personas_cited no longer contains "Agentic AI Architect"; timestamps normalized | Consider a separate `get_project_learnings` MCP tool for explicit internal-memory queries |
