  # Discovery Log

## Purpose

This file records meaningful discoveries that should influence future decisions, implementation, or source tracking.

## Discovery Ledger

| Date | Discovery | Why It Matters | Evidence / Source | Follow-Up |
|------|-----------|----------------|-------------------|-----------|
| 2026-03 | Dynamic tool discovery and load-on-demand MCP patterns are replacing always-on MCP assumptions | Affects how future tool integration and orchestration should be designed; pushes the system toward contract-first, runtime-selectable tools | `Agentics_Ideation_Sesh_ChatGPT45.md`, `docs/phase-3-trends.md`, `docs/phase-4-tools.md` | Reflect in future MCP interface and shared substrate design |
| 2026-03 | The biggest near-term risk is overbuilding the ecosystem before one trustworthy product exists | Prevents roadmap drift into premature platform work | `docs/chatgpt45-ideation-review.md`, `docs/ecosystem-sequencing-memo.md` | Keep AI Architect MVP bounded and query-focused |
| 2026-03 | High-value future systems will share provenance, confidence, evaluation, and schema conventions | Indicates that the second step should be a thin shared substrate, not an immediate second full specialist product | `docs/ecosystem-sequencing-memo.md` | Define shared substrate spec inside this repo before opening the next repo |
| 2026-03 | A user-curated influencer and post watchlist is a high-value discovery input for Phase 1 education | Gives the system a practical source set of practitioners, posts, and themes to review before broadening automated source discovery | `docs/influencer-tracker.md` | Review the captured URLs, summarize high-signal entries, and promote the strongest sources into a top-tier watchlist |
| 2026-03 | Public builder guides can be used to enrich the tracker even when most LinkedIn content is auth-blocked | Allows the source system to gain real reviewed summaries now instead of waiting for full LinkedIn access | `docs/influencer-tracker.md`, `docs/influencer-source-registry.yaml` | Prefer public companion pages, blogs, talks, and newsletters when direct LinkedIn review is blocked |
| 2026-03 | The repo needs an explicit cycle specification, not just a high-level roadmap | The original prompt is cycle-driven, and execution quality depends on having a precise definition of what one good research-and-training iteration produces | `docs/research-training-cycle-v1.md` | Use the cycle spec to drive the next answer-contract, evaluation, and source-weighting work |
| 2026-03 | Readable LinkedIn PDF exports can be deterministically converted into structured post records plus OCR-derived diagram intent at useful scale | Makes a practical ingest boundary for auth-gated social content without requiring live LinkedIn crawling, and is sufficient to trigger downstream research loops | `docs/linkedin-pdf-ingest-pipeline-v1.md`, `data/ingests/Agentic_AI_Architect_Ingest_20260314/centralized_report.md` | Improve repost-chain separation and add rendered-page OCR fallback for visuals not preserved as embedded images |
| 2026-03 | A user-curated likes export is noisy but still high-yield once filtered by architecture relevance and insight score | Lets the system preserve the user's taste as a discovery prior while suppressing friend announcements and low-signal social content | `data/ingests/Agentic_AI_Architect_Ingest_20260314/ingest_result.json`, `data/knowledge_base.db` | Capture filtered-out reasons and calibrate thresholds with more exports |
| 2026-03 | The ExMorbus target is not merely a medical assistant but a MoltBook-like community of medically focused agents exchanging services, tools, evaluations, and research labor | Changes the shape of the future shell, scoring model, and shared substrate by making agent-to-agent selection, feedback, and refinement core system behaviors rather than optional collaboration features | `docs/exmorbus-moltbook-poc-scope-v1.md` | Use the PoC to validate modified social mechanics, routing, and refinement contracts before opening the next specialist repo |
| 2026-03 | The first reusable shared substrate is likely to center on provenance, confidence, evaluation, event envelopes, namespace metadata, and task/result contracts rather than a shared runtime | Helps keep the ecosystem disciplined by extracting only the contracts that are demonstrably reused across AAA and ExMorbus | `docs/shared-substrate-candidate-spec-v0.md`, `docs/ecosystem-sequencing-memo.md` | Pressure-test these contract shapes in the ExMorbus PoC before considering a separate shared repository |
| 2026-03 | Earlier docs-led AAA work on contracts, evaluation, weighting, and durable memory can be reused directly as constraint material for ExMorbus-through-AAA | Prevents the ExMorbus track from becoming an isolated parallel architecture effort and gives the PoC concrete reusable patterns to validate | `docs/first-answer-contract-v0.md`, `docs/initial-eval-question-set-v0.md`, `docs/source-weighting-model-v2.md`, `docs/exmorbus-through-aaa-operating-model-v1.md` | Treat the ExMorbus PoC as the first cross-product pressure test for those patterns |
| 2026-03 | LLM inference quality (70B model, local) is the single highest-leverage hardware investment for the Mouseion/ExMorbus sprint; DGX Spark enables this at ~$3K/unit vs. >$300K for HGX-class hardware | Determines first hardware buy and justifies DGX Spark over cheaper GPU options; 128 GB unified memory is the threshold that allows full 70B model residency | `docs/hardware-scaling-plan-v1.md` | Validate DGX Spark multi-unit Ethernet inference latency before committing to a 10-unit cluster vs. a single HGX node |
| 2026-03 | At 10× agent scale (2,000 agents), SQLite becomes a bottleneck; the migration trigger is >1 M KB entries or >100ms P95 query latency | Gives the team a concrete measured threshold for the PostgreSQL + pgvector migration, preventing premature migration that adds complexity before it is needed | `docs/hardware-scaling-plan-v1.md` | Add a latency probe to the knowledge base to surface this metric in the evaluation performance dashboard |
| 2026-03 | ExMorbus currently proves one bounded live cycle in a snapshot, so native cycle and event persistence is a higher-priority missing layer than MLflow experiment tracking | Prevents the project from confusing experiment analytics with system-of-record runtime history; run truth, lineage, economy deltas, and evaluation outcomes need first-class native storage before optional MLflow overlays add value | `workspaces/ExMorbus-v0.1/apps/web/frontend/data/workbench-snapshot.json`, `workspaces/ExMorbus-v0.1/docs/cycle_history_and_experiment_tracking_v1.md`, `workspaces/ExMorbus-v0.1/README.md` | Implement a PostgreSQL-backed cycle ledger, then project selected run summaries into MLflow for variant comparison later |
| 2026-04 | LinkedIn's virtual DOM aggressively removes off-screen posts during downward scroll, causing 40-50% occlusion loss without a final-top pass | Makes a final scroll-to-top + re-scrape step mandatory in any DOM-based LinkedIn scraper; without it, the first ~20 posts visible at page load are never captured because the DOM removes them before the scraper scrolls back up | Chrome extension v18-v21 captured 29/49 posts; v22 with final-top pass captured 49/49 | Apply the same final-top pattern to any virtual-DOM scrapers for other social platforms (Twitter/X, etc.) |
| 2026-04 | A single ChromaDB collection with `persona_id` + `post_type` metadata fields is sufficient to organize heterogeneous content (LinkedIn posts, YouTube transcripts, GitHub READMEs) for semantic search | Avoids over-engineering multiple collections while enabling persona-filtered, type-filtered, and cross-source queries from one API; `all-MiniLM-L6-v2` embeddings produce coherent cosine similarity across all three content types | 120-item corpus search tests; semantically relevant results returned across persona and content type combinations | Consider collection sharding only if corpus exceeds 1M items or per-persona query latency exceeds 200ms |
| 2026-04 | Claude Code is the dominant tool in current AI practitioner discourse: 48 mentions across 120 indexed items, 2× the next tool (GitHub at 24×) | Provides a concrete, data-backed signal about where the community's attention is; this dominance is primarily driven by Cole Medin's content but also appears independently in LinkedIn posts from unrelated personas | `get_trending_tools()` on full 120-item corpus via `src/api/mcp_server.py` | Track month-over-month tool frequency shifts as new content is ingested to surface emerging tools early |
| 2026-04 | First eval run on 204-item corpus passes 15/15 questions (100%) with avg top relevance 0.465 — the semantic index is already production-quality | Confirms that ChromaDB + all-MiniLM-L6-v2 embeddings on heterogeneous content (blog, YouTube, LinkedIn, arXiv, GitHub) produce semantically coherent retrieval; no tuning needed before Claude Desktop use | `scripts/run_eval.py` first run on `data/wiki/schema/eval_ground_truth.json` | Re-run eval as corpus grows to catch query drift or relevance decay |
| 2026-04 | Simon Willison and Lilian Weng blog posts materially improve coverage of security/prompt injection and RL/evaluation topics that were underrepresented in the LinkedIn-only corpus | Adds two high-signal voices not reachable via LinkedIn scraping; their content density (full blog posts vs. LinkedIn snippets) improves document depth for technical queries | Eval question eval-007 (Willison prompt injection) and eval-013 (Weng reward hacking) both pass with 75%+ score | Continue expanding blog sources; Chip Huyen and Sebastian Ruder are strong candidates |
| 2026-04 | The `research_sources.json` dedup check permanently blocked re-enrichment of posts added in a text-only first pass | Created a silent data-quality problem where the first successful ingest wins even when it contained no Claude extraction; fix requires deleting the old ChromaDB entry and re-ingesting, not re-running the script | April 2026 enrichment pass where all 92 LinkedIn posts were silently skipped by `extract_transcript_sources.py` | Future ingest scripts should check whether existing entries have empty `mentioned_tools` and treat that as a re-enrichment signal, not a dedup hit |
| 2026-06-28 | AAA was recording but not learning — the feedback loop was entirely missing | The system had excellent external knowledge ingestion (260+ items) and solid institutional memory files (decision-log, discovery-log, lessons-learned-log), but none of those internal files were indexed in ChromaDB; `search_knowledge` only searched external corpus; a query about "what did we learn from building ExMorbus?" returned external Karpathy/Weng content, not AAA's own hard-won lessons | Diagnostic review of all four log files and ChromaDB corpus on 2026-06-28 | Close the loop by indexing logs into ChromaDB; wire into weekly refresh; add eval questions targeting project learning retrieval |
| 2026-06-28 | Project learnings score 0.43–0.46 relevance alongside external content — immediately useful with no tuning | First test query "deduplication ingest pipeline" returned top-3 results all from `project_learning` entries; scores comparable to well-enriched blog posts; confirms that plain-text markdown table content embeds well with all-MiniLM-L6-v2 without Claude extraction pre-processing | Sanity test immediately after ingest of 47 entries on 2026-06-28 | Consider a Claude Haiku extraction pass over learning entries to improve topic/tool tagging density; run eval to quantify improvement |
| 2026-06-28 | Adversarial persona discourse produces meaningfully better design decisions than single-voice planning | The CLAUDE.md template design session ran 6 indexed personas with explicit adversarial positions; Willison's "fewer docs = more compliance" pushed back on Weng's "structure is load-bearing," resolving in the DoD-as-enforcement insight; Huyen's "constitution over wiki" replaced full wiki ingestion with a 200-word distillation; these resolutions would not emerge from a single-voice design pass | Board of Governors CLAUDE.md design session 2026-06-28 | Apply adversarial Board session to other design decisions — particularly the P6 self-improvement architecture and ExMorbus adapter design |
| 2026-06-28 | Organic Agentic AutoDev (OAA) already implements Mouseion as working runtime; AAA only had it as contract candidates | OAA (`BraPil/Organic_Agentic_AutoDev`) has a live Mouseion substrate (event bus, resource pools, `KnowledgeRecordV0` with provenance/confidence/review_history, niche registry) plus a full bio-mimicry economy (stem-cell differentiation, slime-mold topology, fitness selection). It credits AAA's Mouseion + MoltBook patterns as its source. This makes OAA the natural runtime for AAA's P6 learning loop and the shared substrate to validate before ExMorbus bets on it | Ingested OAA repo (made public) 2026-06-28; `src/mouseion/`, `docs/architecture.md` | Converge the two Mouseion v0s; do not fork. See `docs/aaa-organic-learning-loop-v0.md` |
| 2026-06-28 | OAA packages itself as top-level `src/`, colliding with AAA's `src/` — forces process isolation (which is the right design anyway) | OAA's `pyproject.toml` sets `[tool.setuptools.packages.find] include = ["src*"]` and ships no console entry point; pip-installing it into AAA would shadow AAA's own `src/` package. The fix doubles as the correct architecture: OAA runs as its own process and emits a KnowledgeRecordV0 JSONL file that AAA harvests — matching the agreed AAA→OAA→AAA boundary (suggest/influence → run → observe/act) | OAA `pyproject.toml` review 2026-06-28 | Before slice 2 live run, OAA should rename package (`src`→`organic_agentic_autodev`) or add a CLI entry point; AAA harvester already consumes the file-based contract |
| 2026-06-28 | OAA is a dynamics simulator, not a cognition engine — the learning loop's hard part is unbuilt | OAA's agents differentiate, cluster, evolve, and die on abstract energy/signal but do NOT call LLMs or perform real research (`[ ] LLM-backed agent cognition` unchecked in roadmap). A real AAA learning loop = OAA lifecycle + LLM research + AAA corpus + regulated promotion; the cognition bridge is the substantive new work | OAA README roadmap + source review 2026-06-28 | Build slices 0–1 (regulation + plumbing) on OAA's deterministic simulation first; cognition bridge (slice 2) is where real value and real cost begin |
